# E12: Remote kernels

Connect to a running Jupyter server over WebSocket (`jupyter-websocket-client`); opens the loop with users on remote/SSH/cloud workflows. Needs slicing into stories before pickup.

Spec: [Future: remote kernels](../../PRD.md#future-remote-kernels)

## Stories

