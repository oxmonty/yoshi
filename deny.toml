[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "MPL-2.0",
    "AGPL-3.0-only",
    "ISC",
    "Unicode-3.0",
    "Zlib",
    # transitive-only exceptions surfaced by the E1 gpui tree (see spikes/gpui-spike/CAPTURES.md)
    "CC0-1.0",                        # hexf-parse (via naga), tiny-keccak (via ahash)
    "NCSA",                           # libfuzzer-sys (via rav1e/ravif/image)
    "0BSD",                           # proc-macro helper, compile-time only
    "BSL-1.0",                        # clipboard-win, Windows-only cfg
    "Apache-2.0 WITH LLVM-exception", # ar_archive_writer (via psm/stacker build dep)
    "Unlicense",                      # win_uds (via zeromq), Windows-only cfg, public-domain dedication
]
